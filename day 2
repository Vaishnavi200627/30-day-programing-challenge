#include<stdio.h>
int main()
{
	int i,j,n,sum=0,expectsum;
	printf("enter n");
	scanf("%d",&n);
	printf("enter elements of array");
	int arr[n];
	for(i=0;i<n;i++){
		scanf("%d",&arr[i]);
			sum=sum+arr[i];
		}
		expectsum=(n*(n+1))/2;
	
	printf("missing element is %d",expectsum-sum);
	return 0;
	}
